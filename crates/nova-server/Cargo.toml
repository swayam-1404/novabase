[package]
name = "nova-server"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Single-node NovaDB server: protocol, connections, timeouts, structured errors, graceful shutdown"

[dependencies]
nova-auth = { path = "../nova-auth", version = "0.1.0" }
nova-core = { path = "../nova-core", version = "0.1.0" }
nova-executor = { path = "../nova-executor", version = "0.1.0" }
nova-query = { path = "../nova-query", version = "0.1.0" }

[lints]
workspace = true
