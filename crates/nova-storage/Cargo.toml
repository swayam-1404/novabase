[package]
name = "nova-storage"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Page-based persistence: page manager, slotted pages, collections, free-space management"

[dependencies]
nova-core = { path = "../nova-core", version = "0.1.0" }
nova-nbf = { path = "../nova-nbf", version = "0.1.0" }

[lints]
workspace = true
