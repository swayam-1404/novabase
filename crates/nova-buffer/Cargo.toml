[package]
name = "nova-buffer"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Bounded buffer pool for paging between the storage engine and disk"

[dependencies]
nova-core = { path = "../nova-core", version = "0.1.0" }
nova-storage = { path = "../nova-storage", version = "0.1.0" }

[lints]
workspace = true
