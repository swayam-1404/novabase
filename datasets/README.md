# Datasets

Benchmark datasets are generated rather than committed. Follow
[`workload-spec.md`](workload-spec.md) for deterministic 10k, 100k, 500k, and
1M-document workloads and record the seed with every result set.
